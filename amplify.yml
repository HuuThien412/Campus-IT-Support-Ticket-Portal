version: 1
frontend:
  phases:
    build:
      commands:
        - node scripts/write-runtime-config.mjs
        - hugo --minify
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: []